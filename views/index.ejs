<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ejs1</title>
</head>

<body>
    <h1><%= title %></h1>
    <p>Current second: <%= sec %></p>
    
    <%# if even then list items %>
    <% if(sec%2===0){ %>
        <ul>
            <%for(var i=0;i<items.length;i++){%>
                <li><%= items[i] %></li>
            <% }%>
        </ul>
    <%}else{ %>
        <p>No items to display</p>
        <% } %>
    <%# if odd then no items to display %>
    
    <%- htmlContent %>
    
    <%- include("footer.ejs") %>
    
</body>

</html>